import Layout from "../../components/layout/layout";
import {Container, Typography} from "@mui/material";

const TicketInfoPage = () => {

    return (
        <Layout>
            <Container>
                <Typography variant="h6">Ticket Info Page</Typography>
            </Container>
        </Layout>
    )
}

export default TicketInfoPage;
